syntax = "proto3";
package Jde.App.Proto;

message Execute{
	uint32 user_pk = 1;
	bytes transmission = 2;
}

message StringMD5s{
	repeated bytes messages = 1;
	repeated bytes files = 2;
	repeated bytes functions = 3;
	repeated uint32 userPKs = 5; //targets for web.
}
