#pragma once
#include "AuthAwait.h"

namespace Jde::Opc::Gateway{
/*	struct TokenAwait : AuthAwait<bool>, boost::noncopyable{
		TokenAwait( string token, ServerCnnctnNK opcNK, string endpoint, bool isSocket, SRCE )ι:
			AuthAwait{ move(token), move(opcNK), move(endpoint), isSocket, sl }{}
		α Suspend()ι->void override{ Execute(); }
		α OnSuccess()ι->void override{ ResumeScaler( true ); }
	};
*/
}