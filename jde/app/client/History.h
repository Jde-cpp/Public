#pragma once
#include "usings.h"

namespace Jde::App{
/*
	struct History{

		α AddMessage( Hash messageId )ι->bool{ return _messagesSent.emplace(messageId); }
		α AddFile( Hash messageId )ι->bool{ return _filesSent.emplace(messageId); }
		α AddFunction( Hash messageId )ι->bool{ return _functionsSent.emplace(messageId); }
		α AddUser( Hash messageId )ι->bool{ return _usersSent.emplace(messageId); }
		α AddThread( Hash messageId )ι->bool{ return _threadsSent.emplace(messageId); }
		concurrent_flat_set<Hash> _messagesSent;
		concurrent_flat_set<Hash> _filesSent;
		concurrent_flat_set<Hash> _functionsSent;
		concurrent_flat_set<Hash> _usersSent;
		concurrent_flat_set<Hash> _threadsSent;
	};
*/
}