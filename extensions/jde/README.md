# Utilities

  - jde.repoBuildDir Appends the repository directory name to the build directory path

# Formatter

This Formatter adds a space [after first]|[before last] bracket.  No spaces inbetween.