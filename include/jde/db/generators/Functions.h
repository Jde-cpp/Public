#pragma once
//Coalesce/Count now live in Object.h so the Object variant never sees them as incomplete; kept as an alias for existing #include<jde/db/generators/Functions.h> users.
#include "Object.h"
